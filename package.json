{
  "name": "inventro-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"npx mocha tests/firestore.rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "latest",
    "firebase": "latest",
    "firebase-tools": "latest",
    "mocha": "latest"
  }
}
